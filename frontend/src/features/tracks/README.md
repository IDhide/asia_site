# Страница треков с 3D слайдером

## Описание

Полностью переработанная страница треков с современным дизайном:
- Переключатель Треки/Альбомы вверху
- Большой 3D слайдер с обложками в центре (без стрелок и точек)
- Информационная плашка внизу с названием, исполнителем и кнопками
- Модальное окно с деталями трека и текстом песни

## Особенности

- ✨ 3D эффект coverflow с плавными переходами
- 🎵 Центральная карточка с фокусом
- 🌫️ Размытие боковых карточек
- 📱 Полностью адаптивный дизайн
- ⌨️ Поддержка клавиатуры (стрелки влево/вправо)
- 🎨 Стеклянный эффект (glassmorphism)
- 📝 Модальное окно с текстом песни
- 🔄 Синхронизация текущего трека
- 🎯 Клик по обложке открывает детали
- 🎼 6 кнопок музыкальных сервисов

## Использование

```tsx
import { ReleasesSlider } from '@/features/tracks/components/ReleasesSlider';

const tracks = [
  {
    id: 1,
    title: 'Название трека',
    artist: 'Асия',
    cover: '/path/to/cover.jpg',
    audio: '/path/to/audio.mp3',
    lyrics: 'Текст песни...',
    order: 1
  }
];

<ReleasesSlider
  tracks={tracks}
  onSlideChange={(index) => console.log('Текущий трек:', index)}
/>
```

## Структура компонентов

```
features/tracks/
├── components/
│   ├── ReleasesSlider/
│   │   ├── index.tsx          # 3D слайдер без стрелок и точек
│   │   └── style.module.scss  # Стили с 3D эффектами
│   ├── TrackInfo/
│   │   ├── index.tsx          # Плашка с инфо и кнопками
│   │   ├── style.module.scss  # Стили плашки
│   │   └── README.md          # Документация
│   ├── TrackDetailModal/
│   │   ├── index.tsx          # Модальное окно с деталями
│   │   ├── style.module.scss  # Стили модалки
│   │   └── README.md          # Документация
│   ├── TrackCard/             # Карточка трека (legacy)
│   └── TrackList/             # Список треков (legacy)
└── index.ts                   # Экспорты
```

## Компоненты

### 1. ReleasesSlider
3D слайдер с обложками треков. Без стрелок и точек навигации.
- Клик по обложке открывает модальное окно
- Свайп для переключения треков
- Клавиатурная навигация (стрелки)

### 2. TrackInfo
Информационная плашка с:
- Названием трека
- Исполнителем
- Кнопкой "Тт" (открывает модалку, если есть текст)
- 6 кнопками музыкальных сервисов

### 3. TrackDetailModal
Модальное окно с деталями трека:
- **Desktop**: обложка слева, текст справа
- **Mobile**: обложка сверху, текст снизу
- Закрывается по Escape или клику на overlay

## Настройка Swiper

Компонент использует следующие модули Swiper:
- `EffectCoverflow` - 3D эффект
- `Keyboard` - управление клавиатурой

### Параметры coverflow эффекта:

```typescript
coverflowEffect: {
  rotate: 50,        // Угол поворота боковых слайдов
  stretch: 0,        // Растяжение между слайдами
  depth: 120,        // Глубина 3D эффекта
  modifier: 1,       // Модификатор эффекта
  slideShadows: false // Отключение теней
}
```

## Адаптивность

- **Desktop (>900px)**: Полноразмерные карточки с эффектом размытия по краям
- **Tablet (560-900px)**: Средние карточки, кнопки по краям экрана
- **Mobile (<560px)**: Компактные карточки, упрощенная навигация

## Стили

Компонент использует CSS переменные для гибкой настройки:

```scss
--slide-w: clamp(320px, 26vw, 520px);  // Ширина слайда
```

### Ключевые стили:

- Glassmorphism эффект для кнопок и метаданных
- Плавные cubic-bezier переходы
- Размытие боковых элементов (blur + opacity)
- Адаптивные размеры через clamp()

## Доступность

- ARIA метки для навигации
- Поддержка клавиатуры
- Семантическая разметка
- Prefers-reduced-motion поддержка

## Производительность

- Динамический импорт Swiper
- Lazy loading изображений
- Will-change для оптимизации анимаций
- CSS containment для изоляции стилей
